import React from 'react'

const DeckFilter = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            <h1>Deck  Filter</h1>
            {children}
        </div>
    )
}

export default DeckFilter